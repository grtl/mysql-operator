apiVersion: v1
kind: Service
metadata:
  name: {{ .Spec.Name }}
  labels:
    app: {{ .Spec.Name }}
  ownerReferences:
    - kind: MySQLCluster
      apiVersion: cr.mysqloperator.grtl.github.com/v1
      name: {{ .Name }}
      uid: {{ .UID }}
spec:
  ports:
  - name: mysql
    port: 3306
  clusterIP: None
  selector:
    app: {{ .Spec.Name }}
